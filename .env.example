# ==============================================================================
# AMATEUR CYCLING STATS - ENVIRONMENT CONFIGURATION
# ==============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!
# ==============================================================================

# ------------------------------------------------------------------------------
# APPLICATION CONFIGURATION
# ------------------------------------------------------------------------------
# The base URL where your application is running
# Development: http://127.0.0.1:5173 or http://localhost:5173
# Production: https://yourdomain.com
SITE_URL=http://127.0.0.1:5175

# Development server port (worktree-two uses 5175)
# Main: 5173, worktree-one: 5174, worktree-two: 5175, worktree-three: 5176
DEV_SERVER_PORT=5175

# ------------------------------------------------------------------------------
# SUPABASE CONFIGURATION
# ------------------------------------------------------------------------------
# Choose ONE configuration below based on your environment:

# --- LOCAL DEVELOPMENT (Supabase CLI) ---
# Used when running Supabase locally with `npm run supabase:start`
SUPABASE_URL=http://127.0.0.1:56321
SUPABASE_ANON_KEY=your-local-anon-key-from-supabase-start
SUPABASE_SERVICE_ROLE_KEY=your-local-service-role-key-from-supabase-start

# Supabase Local Instance Ports (worktree-two configuration)
SUPABASE_API_PORT=56321
SUPABASE_DB_PORT=56322
SUPABASE_DB_SHADOW_PORT=56320
SUPABASE_STUDIO_PORT=56323
SUPABASE_INBUCKET_PORT=56324
SUPABASE_POOLER_PORT=56329
SUPABASE_ANALYTICS_PORT=56327

# --- REMOTE/PRODUCTION (Supabase Cloud) ---
# Comment out the local config above and uncomment these for remote development or production
# Get these values from your Supabase project dashboard:
# https://supabase.com/dashboard/project/YOUR_PROJECT_REF/settings/api
#
# SUPABASE_URL=https://your-project-ref.supabase.co
# SUPABASE_ANON_KEY=your-anon-key-from-supabase-dashboard
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-from-supabase-dashboard

# ------------------------------------------------------------------------------
# MAILERSEND CONFIGURATION
# ------------------------------------------------------------------------------
# MailerSend is used for sending custom transactional emails (invitations, etc.)
# Get your API key from: https://www.mailersend.com/settings/api-keys

# Your MailerSend API key (starts with "mlsn.")
MAILERSEND_API_KEY=mlsn.your-api-key-here

# Sender email address (must be verified in MailerSend dashboard)
# Example: noreply@yourdomain.com
MAILERSEND_FROM_EMAIL=noreply@yourdomain.com

# Display name that appears in recipient's inbox
# Example: "Amateur Cycling Stats" or "ACS Team"
MAILERSEND_FROM_NAME=Amateur Cycling Stats

# ==============================================================================
# GITHUB SECRETS REQUIRED FOR CI/CD
# ==============================================================================
# These secrets must be configured in GitHub repository settings:
# GitHub.com → Your Repo → Settings → Secrets and variables → Actions
#
# SUPABASE_ACCESS_TOKEN       - Your personal Supabase access token
#                               Generate at: https://supabase.com/dashboard/account/tokens
#
# SUPABASE_DB_PASSWORD        - Database password for the Supabase project
#                               Find in: Supabase Dashboard → Project Settings → Database
#
# SUPABASE_SERVICE_ROLE_KEY   - Service role key for seeding operations
#                               Find in: Supabase Dashboard → Project Settings → API
#
# Note: These are NOT stored in .env file - they're GitHub repository secrets
# ==============================================================================
